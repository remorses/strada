---
'metrics-ui': patch
'strada-website': patch
---

Turn `metrics-ui` into a design mockup of the Strada dashboard with fake data: Issues, Issue detail, Traces, Logs, Health checks and Analytics tabs, a natural language filter input on every page, and a Linear-style TanStack table ported from celato. Website inputs and buttons now show focus rings only on keyboard focus.
