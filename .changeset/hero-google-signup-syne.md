---
'strada-website': minor
---

Refresh the landing hero and Holocron docs site.

The homepage now uses **Syne** for heading and body type, matching Input.so metrics (76px, weight 600, tracking -0.02em). The hero CTA is **Sign up with Google** with the Google mark. The background is Holocron's later 10s color-corrected video, darkened with ffmpeg, inverted in light mode, and blended with multiply/screen so it sits on the page.

`/signup` redirects to `/login`. `/pricing` redirects to Tinybird pricing.
